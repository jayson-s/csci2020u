plugins{
	id "application"
}

apply plugin: 'java'

mainClassName = "gradleDemo.demo"

 repositories {
    mavenCentral()
 }
​
 dependencies {
   compile group: 'commons-validator', name: 'commons-validator', version: '1.4.1'
 }